# +--------------+
# | Custom Alias |
# +--------------+

alias ll="ls -alF"
alias la="ls -A"
alias l="ls -CF"

# +----------------------+
# | GitHub Copilot Alias |
# +----------------------+

eval "$(gh copilot alias -- zsh)"
